// In Chakra UI v3, theming is handled differently
// The theme is now configured through the system and tokens
// For now, we'll use an empty object as the theme is handled via ColorModeProvider
export const theme = {};
